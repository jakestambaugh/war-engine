use super::input::GameInputEvent;
use super::state::GameState;

pub fn _make_state_machine() -> &'static str {
    "Hello I'm a state machine"
}

fn _generate_next_state(
    _state: &mut GameState,
    _a_input: &GameInputEvent,
    _b_input: &GameInputEvent,
) {
    unimplemented!()
}
